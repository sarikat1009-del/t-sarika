mkdir 3vc24cs140
ls
cd 3vc24cs140
to create a repository
git init
git config-list
git config--global user.name"t-sarika"
git config--global user.email"sarikat1009@gmail.com
git clone
cd 3vc24cd140
git status
$ echo "hello git and git hub" >> labl.txt
$ git add label.txt
$ git status
git commit -m "first msg"
git push origin main
